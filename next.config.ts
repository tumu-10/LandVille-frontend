import type { NextConfig } from "next";

const nextConfig: NextConfig = {

  images: {
    domains: ['127.0.0.1', 'localhost'], // Add your local server's hostname here
  },
  /* config options here */
};

export default nextConfig;
